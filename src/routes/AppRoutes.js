// src/routes/AppRoutes.js
// This file is kept for future routing needs but currently unused
// The portfolio uses single-page scrolling structure in App.js

import React from 'react';

// Placeholder component for future routing implementation
const AppRoutes = () => {
  return (
    <div>
      {/* 
        This component is currently not used as the portfolio 
        implements single-page scrolling navigation.
        
        If you want to implement separate routes in the future:
        1. Uncomment the routing structure below
        2. Update App.js to use this component
        3. Update Navbar.js to use Link components instead of anchor tags
      */}
    </div>
  );
};

export default AppRoutes;
